# 遥感建筑物变化检测项目交接文档

## 一、项目概述

本项目基于 **Ultralytics YOLO 框架（已修改）**，用于拉萨地区无人机影像的建筑物实例分割与变化检测（违建监测）。核心流程：YOLO 实例分割 → 跨时期建筑物匹配 → 变化分类（新增/消失/扩建）→ 输出 Shapefile + GeoTIFF + 报告。

### 项目目录结构

```
ultralytics-main/
├── train_building.py          # 当前训练脚本（v5, 5类分割）
├── train_building_mixed.py    # 当前训练脚本（v5_mixed, 2类 building+car）
├── detect_changes.py          # 当前变化检测脚本（可视化版）
├── preprocess.py              # 当前预处理脚本（v5, 5类）
├── preprocess_mixed.py        # 当前预处理脚本（v5_mixed, 2类）
├── api_app/                   # Flask API 服务（部署版变化检测）
│   ├── app.py                 # Flask 入口
│   ├── config.py              # 配置（端口9030, 阈值等）
│   ├── models/                # 模型加载 + 配置
│   ├── routes/                # API 路由
│   ├── services/              # 变化检测核心服务
│   └── utils/                 # GDAL工具/任务管理/文件工具
├── archive/                   # 历史版本脚本
├── ultralytics/               # 修改后的 ultralytics 框架
│   ├── cfg/models/11/yolo11-star.yaml  # YOLO11-StarNet 模型配置
│   ├── cfg/models/v8/yolov8-star.yaml  # YOLOv8-StarNet 模型配置
│   ├── nn/modules/block.py             # 新增 StarBlock/C2fStar/C3k2Star
├── runs/                      # 训练结果
│   ├── building_seg_v5/       # 当前最佳模型（5类, YOLO11n-seg）
│   ├── building_seg_v5_mixed/ # 2类模型（building+car）
│   └── segment/train-3/       # 早期1类模型
├── change_results/            # 变化检测可视化结果
├── deploy_pack.sh             # 部署打包脚本
└── building_detect_deploy.tar.gz  # 预打包部署文件
```

---

## 二、模型训练过程

### 2.1 训练流程总览

```
原始标注数据 (LabelMe JSON) → preprocess.py 转换为 YOLO 格式 → data.yaml 配置 → train_building.py 训练 → runs/ 保存权重
```

### 2.2 数据预处理

预处理脚本将 LabelMe JSON 标注转换为 YOLO 分割格式（多边形坐标归一化），同时划分训练/验证/测试集。

| 预处理脚本 | 输出数据集 | 类别 | 划分方式 | 说明 |
|---|---|---|---|---|
| `preprocess.py` | building_seg_v5 | 5类（见下方） | 随机 80/10/10 | 仅使用新标注数据 |
| `preprocess_mixed.py` | building_seg_v5_mixed | 2类（building/car） | 按地点分组划分 | 新旧数据混合，防止数据泄漏 |

**preprocess.py 关键配置：**
- 数据源：`/home/fumu/datadisk/标注区域划分/堆龙东嘎` + `/home/fumu/datadisk/标注区域划分/曲水`
- 输出：`/home/fumu/datadisk/building_seg_v5`
- MIN_POINTS = 4（多边形最少4个点才保留）
- 5类映射：residential(0), steel_roof(1), large_complex(2), glass_roof(3), under_construction(4)

**preprocess_mixed.py 关键配置：**
- 新数据：堆龙东嘎 + 曲水（5类建筑 → 统一映射为 building）
- 旧数据：`/home/fumu/datadisk/drone_data_src`（building + car）
- 输出：`/home/fumu/datadisk/building_seg_v5_mixed`
- 按地点分组划分（堆龙东嘎/曲水/旧数据各自分 train/val/test，防止同地点影像泄漏）

### 2.3 训练参数

两个当前训练脚本均使用以下参数：

| 参数 | 值 |
|---|---|
| 模型 | YOLO11n-seg（预训练权重 yolo11n-seg.pt） |
| epochs | 100 |
| imgsz | 640 |
| batch | 16 |
| patience | 50 |
| lr0 | 0.01 |
| overlap_mask | True |
| mask_ratio | 4 |
| 数据增强 | 默认（mosaic=1.0, fliplr=0.5, auto_augment=randaugment） |

---

## 三、不同数据集训练要改哪里的代码

### 3.1 切换数据集需要修改的位置

#### train_building.py

```python
# 需修改的3个关键变量：
MODEL_WEIGHTS = "yolo11n-seg.pt"           # 模型预训练权重
DATA = "/home/fumu/datadisk/building_seg_v5/data.yaml"  # 数据集配置路径
EXPERIMENT_NAME = "building_seg_v5"         # 实验名称（影响输出目录）
```

#### train_building_mixed.py

```python
# 同样修改这3个变量：
MODEL_WEIGHTS = "yolo11n-seg.pt"
DATA = "/home/fumu/datadisk/building_seg_v5_mixed/data.yaml"
EXPERIMENT_NAME = "building_seg_v5_mixed"
```

#### data.yaml（数据集配置文件）

```yaml
path: /home/fumu/datadisk/building_seg_v5      # 数据集根目录
train: images/train                              # 训练集图片目录
val: images/val                                  # 验证集图片目录
test: images/test                                # 测试集图片目录
nc: 5                                            # 类别数量（必须与标签一致）
names:
  0: residential
  1: steel_roof
  2: large_complex
  3: glass_roof
  4: under_construction
```

### 3.2 如果想换模型架构（如 StarNet）

需修改 `MODEL_WEIGHTS` 和添加 `cfg` 参数：

```python
# 使用 YOLO11-StarNet（自定义架构）
model = YOLO("ultralytics/cfg/models/11/yolo11-star.yaml")
model.train(data=DATA, ...)  # 从头训练，无预训练权重

# 或用预训练 YOLO11n-seg 加载后再指定 cfg
# 注意：StarNet 架构与标准 YOLO11 不同，不能直接从 yolo11n-seg.pt 加载
```

### 3.3 preprocess.py / preprocess_mixed.py 需修改的位置

如果新增标注数据或更换数据源：

```python
# preprocess.py:
SOURCE_DIRS = [
    "/home/fumu/datadisk/标注区域划分/堆龙东嘎",   # 数据源目录
    "/home/fumu/datadisk/标注区域划分/曲水",       # 数据源目录
]
DEST_DIR = "/home/fumu/datadisk/building_seg_v5"  # 输出目录
MIN_POINTS = 4                                     # 最少多边形点数
CLASS_MAP = {                                      # 类别映射（LabelMe标签→YOLO类别ID）
    "residential": 0,
    "steel_roof": 1,
    "large_complex": 2,
    "glass_roof": 3,
    "under_construction": 4,
}
```

---

## 四、训练脚本及命令行训练方法

### 4.1 当前训练脚本

| 脚本 | 对应数据集 | 类别 | 训练结果目录 |
|---|---|---|---|
| `train_building.py` | building_seg_v5 | 5类建筑细分 | runs/building_seg_v5 |
| `train_building_mixed.py` | building_seg_v5_mixed | 2类(building+car) | runs/building_seg_v5_mixed |

### 4.2 Python 脚本方式训练

```bash
cd /home/fumu/PycharmProjects/ultralytics-main

# 5类分割训练
python train_building.py

# 2类(building+car)混合训练
python train_building_mixed.py
```

### 4.3 命令行方式训练（使用 ultralytics CLI）

```bash
cd /home/fumu/PycharmProjects/ultralytics-main

# 5类分割训练（与 train_building.py 等效）
yolo segment train data=/home/fumu/datadisk/building_seg_v5/data.yaml model=yolo11n-seg.pt epochs=100 imgsz=640 batch=16 patience=50 project=/home/fumu/PycharmProjects/ultralytics-main/runs name=building_seg_v5 lr0=0.01 overlap_mask=True mask_ratio=4

# 2类混合训练（与 train_building_mixed.py 等效）
yolo segment train data=/home/fumu/datadisk/building_seg_v5_mixed/data.yaml model=yolo11n-seg.pt epochs=100 imgsz=640 batch=16 patience=50 project=/home/fumu/PycharmProjects/ultralytics-main/runs name=building_seg_v5_mixed lr0=0.01 overlap_mask=True mask_ratio=4

# 使用 StarNet 架构训练（无预训练权重）
yolo segment train data=/home/fumu/datadisk/building_seg_v5/data.yaml model=ultralytics/cfg/models/11/yolo11-star.yaml epochs=100 imgsz=640 batch=16 project=runs name=building_seg_YOLO11Star
```

### 4.4 预处理命令

```bash
# 5类数据预处理
python preprocess.py

# 2类混合数据预处理
python preprocess_mixed.py
```

---

## 五、不同训练脚本之间的区别及对应结果

### 5.1 训练脚本演进对比

| 版本 | 脚本 | 数据集 | 类别 | imgsz | epochs | 增强策略 | 划分方式 | 最佳 mAP50-95(B) |
|---|---|---|---|---|---|---|---|---|
| v1 | archive/train_building.py | drone_seg_dataset | 1类(building) | 640 | 200 | 重度增强(mosaic/mixup/flip) | 随机 | - |
| v2 | archive/train_building_v2.py | building_seg_dataset_v3 | 1类(building) | 640 | 100 | 默认 | 时间分组 | - |
| v3 | archive/train_building_5class.py | building_seg_dataset | 5类 | 1024 | 200 | 重度增强 | 随机 | - |
| **v5** | **train_building.py** | **building_seg_v5** | **5类** | 640 | 100 | 默认 | 随机80/10/10 | **0.5626** |
| **v5_mixed** | **train_building_mixed.py** | **building_seg_v5_mixed** | **2类(building+car)** | 640 | 100 | 默认 | 地点分组 | 0.2941 |

### 5.2 v5 与 v5_mixed 的核心区别

| 对比项 | v5 (train_building.py) | v5_mixed (train_building_mixed.py) |
|---|---|---|
| 类别数 | 5类（residential/steel_roof/large_complex/glass_roof/under_construction） | 2类（building/car） |
| 数据来源 | 仅新标注数据（堆龙东嘎+曲水） | 新旧数据混合（堆龙东嘎+曲水+旧数据238张） |
| 划分方式 | 随机80/10/10 | 按地点分组（防数据泄漏） |
| 样本数 | 478张（train380/val49/test49） | 726张（train572/val129/test25） |
| 标注框数 | 3,867 | 30,634（building 11,977 + car 18,657） |
| 训练结果 | best epoch 79, mAP50(B)=0.71, mAP50-95(B)=0.56 | best epoch 76, mAP50(B)=0.49, mAP50-95(B)=0.29 |
| 权重路径 | runs/building_seg_v5/weights/best.pt | runs/building_seg_v5_mixed/weights/best.pt |

**注意：v5_mixed 的 mAP 较低，主要原因是 car 类别样本量极大（18,657）远超 building（11,977），导致类别不平衡问题严重。**

### 5.3 各脚本对应的建筑物变化检测结果

| 训练脚本/模型 | 变化检测用途 | 对应结果目录 |
|---|---|---|
| v5 (YOLO11n-seg, 5类) | **当前部署模型**，API服务使用，5类细分可区分建筑类型变化 | runs/building_seg_v5 + api_app/api_results |
| v5_mixed (YOLO11n-seg, 2类) | 可检测 building 和 car，但精度较低，未部署 | runs/building_seg_v5_mixed |
| segment/train-3 (早期1类) | 早期变化检测测试（乃琼区域5对对比） | change_results/YOLO11n-seg/ |
| StarNet 系列 | 仅做了检测（非分割）对比实验，未用于变化检测 | runs/building_detect_YOLO11Star 等 |

### 5.4 变化检测结果说明

**change_results/ 目录包含多组对比结果：**

| 子目录 | 内容 | 说明 |
|---|---|---|
| `change_results/` (根目录) | ~44张PNG对比图 + detect_report.txt | v4版本结果，东嘎/乃琼多时期对比 |
| `change_results/change_detection/` | ~44张PNG + report | 堆龙东嘎/曲水 TIFF对比（标注区域数据） |
| `change_results/YOLO11n-seg/` | 5张PNG + report | 早期 train-3 模型结果，含 mask IoU |
| `change_results/predictions/` | 25张预测图 + predict_report.txt | 单图预测（非变化检测） |

**可视化标记含义：**
- 绿色 = 已存在建筑（两期均检测到）
- 红色 = 新增建筑
- 紫色 = 消失建筑
- 黄色 = 车辆（仅2类模型）

---

## 六、数据集详细信息

本项目共涉及三个数据集：第一批数据（2类 building+car）、第二批数据（5类建筑细分）、混合数据集（两者合并）。

### 6.1 数据集一：第一批数据（building + car，2类）

**原始数据路径（多个副本，内容基本一致）：**
- `/home/fumu/datasets/无人机分割数据集/` — 240张 JPG + LabelMe JSON（最完整版本）
- `/home/fumu/PycharmProjects/无人机分割数据集/` — 212张 JPG + 210个 JSON
- `/home/fumu/datadisk/drone_data_src/` — 238张 JPG + LabelMe JSON（用于预处理mixed数据集的版本）

**特点：** 第一批标注数据，只有 building 和 car 两个类别，地点覆盖较广（东嘎、乃琼、曲水、羊达、聂当、古荣等多个拉萨郊区区域）。

| 项目 | 数值 |
|---|---|
| 地点 | 拉萨郊区多个地点（东嘎、乃琼、曲水、羊达、聂当、古荣等） |
| 总图片数 | ~240张 |
| 类别数 | 2（building, car） |
| 总标注框数 | ~29,621 |

**各类别分布：**

| 类别ID | 类别名 | 数量 | 占比 |
|---|---|---|---|
| 0 | building | ~8,148 | 27.4% |
| 1 | car | ~21,473 | 72.6% |

**处理后数据集版本：**

| 版本 | 路径 | 类别 | train | val | test | 说明 |
|---|---|---|---|---|---|---|---|
| drone_seg_dataset | `/home/fumu/datasets/drone_seg_dataset/` | 1类(building) | 181 | 45 | 10 | 早期只取building，去掉car |
| drone_seg_dataset_v4 | `/home/fumu/datadisk/drone_seg_dataset_v4/` | 2类(building+car) | 185 | 21 | 25 | v4版本 |
| drone_seg_v5 | `/home/fumu/datadisk/drone_seg_v5/` | 2类(building+car) | 190 | 22 | 24 | v5版本 |

### 6.2 数据集二：第二批数据（5类建筑细分）

**原始数据路径：**
- `/home/fumu/datadisk/标注区域划分/堆龙东嘎/` — 383张 PNG + LabelMe JSON
- `/home/fumu/datadisk/标注区域划分/曲水/` — 111张 PNG + LabelMe JSON

**特点：** 第二批标注数据，将建筑细分为5个类别，标注更精细。仅覆盖堆龙东嘎和曲水两个地点。

**处理后数据集路径：** `/home/fumu/datadisk/building_seg_v5/`

| 项目 | 数值 |
|---|---|
| 地点 | 拉萨市堆龙德庆区东嘎镇 + 曲水县 |
| 总图片数 | 494张（堆龙东嘎383 + 曲水111） |
| 训练集 | 380张 |
| 验证集 | 49张 |
| 测试集 | 49张 |
| 类别数 | 5 |
| 总标注框数 | 3,867 |

**各类别分布：**

| 类别ID | 类别名 | 堆龙东嘎 | 曲水 | 合计 | 占比 |
|---|---|---|---|---|---|
| 0 | residential（住宅） | 865 | 940 | 1,805 | 46.7% |
| 1 | steel_roof（彩钢棚） | 1,166 | 570 | 1,736 | 44.9% |
| 2 | large_complex（大型综合体） | 218 | 170 | 388 | 10.0% |
| 3 | glass_roof（玻璃顶） | 217 | 70 | 287 | 7.4% |
| 4 | under_construction（在建） | 14 | 4 | 18 | 0.5% |

**注：以上为原始标注中的类别分布，预处理后转换到YOLO格式时数量可能有微小差异（MIN_POINTS=4过滤）。**

### 6.3 数据集三：混合数据集（第一批+第二批合并）

**原始数据来源：**
- 第一批数据：`/home/fumu/datadisk/drone_data_src/` — 238张 JPG + LabelMe JSON（building + car）
- 第二批数据：堆龙东嘎（383张）+ 曲水（111张）→ building 类别（5类建筑合并为building）

**特点：** 将两批数据合并，第一批的 building 类别和第二批的5类建筑统一映射为 building，保留了第一批的 car 类别。按地点分组划分防止数据泄漏。

**处理后数据集路径：** `/home/fumu/datadisk/building_seg_v5_mixed/`

| 项目 | 数值 |
|---|---|
| 地点 | 东嘎、乃琼、曲水、羊达、聂当、古荣等多地点 |
| 总图片数 | 732张（第一批238 + 堆龙东嘎383 + 曲水111） |
| 训练集 | 572张 |
| 验证集 | 129张 |
| 测试集 | 25张 |
| 类别数 | 2（building, car） |
| 总标注框数 | 30,634 |

**各类别分布：**

| 类别ID | 类别名 | 第一批数据 | 第二批数据（5类→building） | 合计 | 占比 |
|---|---|---|---|---|---|
| 0 | building | 8,095 | ~4,234 | ~11,977 | 39.1% |
| 1 | car | 21,353 | 0 | 18,657 | 60.9% |

### 6.4 数据集不足与改进方向

| 问题 | 说明 | 改进建议 |
|---|---|---|
| 类别不平衡 | 第一批 car 占72.6%，混合数据集 car 占60.9%；第二批 under_construction 仅0.5% | 增加稀有类别标注；考虑用类别权重或Focal Loss；减少car样本比例 |
| 第一批数据 car 过多 | 21,353个 car 远超 8,148个 building，导致模型偏向 car 检测 | 对 car 类做欠采样，或单独训练 building 模型 |
| 第二批数据量不足 | 仅494张图片，3,867个框，地点仅2处 | 扩大标注范围，增加更多地点/时期数据 |
| 地点覆盖不均 | 第一批覆盖多地点但标注粗糙（只有building+car），第二批标注精细但仅2个地点 | 统一标注标准，将第一批数据也做5类细分标注 |
| 在建建筑极少 | under_construction 仅18个样本 | 重点补充在建工地标注 |
| 划分方式不统一 | 第二批(v5)用随机划分可能数据泄漏，混合数据集已改进 | 统一使用地点分组划分 |
| 影像时期覆盖 | 两批数据均为单一时期 | 需补充多时期标注数据以改善变化检测鲁棒性 |
| 混合数据集精度低 | mAP50-95(B)仅0.29，car类别严重干扰building检测 | 减少car样本比例或单独训练building模型 |
| 第一批数据地点信息缺失 | 原始数据未明确标注各图片的拍摄地点 | 补充元数据，标注每张图片的拍摄地点和时期 |

---

## 七、变化检测完整流程

### 7.1 detect_changes.py（可视化版）

用于本地批量对比，生成PNG可视化。

```bash
cd /home/fumu/PycharmProjects/ultralytics-main
python detect_changes.py
```

流程：
1. 从 `/home/fumu/datadisk/变化` 发现时期对比对
2. 对每对影像分别做 YOLO 推理（conf=0.5）
3. IoU 匹配两期建筑物（bbox IoU + mask IoU）
4. 分类：新增（红）/消失（紫）/已存在（绿）
5. 生成对比PNG + 文字报告

### 7.2 api_app（部署版）

用于生产环境，接受 TIFF 上传，输出 Shapefile + GeoTIFF + ZIP。

```bash
cd /home/fumu/PycharmProjects/ultralytics-main
bash api_app/start.sh
# 或直接：python api_app/app.py
```

**API 接口：**

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/images/change` | POST | 提交变化检测任务（上传两期TIFF） |
| `/api/images/change/status/<task_id>` | GET | 查询任务状态 |
| `/api/images/change/result/<task_id>` | GET | 下载结果ZIP |
| `/api/images/segment` | POST | 提交分割任务 |

**部署版变化检测完整流程（change_service.py）：**
1. GDAL 计算两期 TIFF 重叠区域
2. Warp + Crop 到重叠区（目标分辨率 0.2m/px）
3. 读取为 BGR uint8
4. YOLO 分块推理（4096px 大块，1024px 小块，80% stride）
5. 跨块 NMS 合并
6. IoU 匹配两期建筑物
7. 变化分类（新增/消失/扩建，面积变化阈值20%）
8. 输出：JSON + 对比图 + GeoTIFF mask + Shapefile + 文字报告 + ZIP

**关键配置（config.py）：**
- 默认置信度阈值：0.3
- 默认 IoU 阈值：0.5
- 匹配 IoU 阌值：0.5
- 面积变化阈值：0.2（20%）
- 大块尺寸：4096，小块尺寸：1024

---

## 八、自定义模型架构（StarNet）

本项目在 ultralytics 框架中新增了 StarNet 模块（论文：Rewrite the Stars, arXiv:2403.19967）：

| 模块 | 替换对象 | 用途 |
|---|---|---|
| StarBlock | Bottleneck | 元素级乘法特征融合 + 深度可分离卷积 |
| C2fStar | C2f (YOLOv8) | 用 StarBlock 替换 C2f 中的 Bottleneck |
| C3k2Star | C3k2 (YOLO11) | 用 StarBlock 替换 C3k2 中的子模块 |

**模型配置文件：**
- `ultralytics/cfg/models/11/yolo11-star.yaml` — YOLO11-StarNet
- `ultralytics/cfg/models/v8/yolov8-star.yaml` — YOLOv8-StarNet

**代码修改位置：**
- `ultralytics/nn/modules/block.py` — StarBlock/C2fStar/C3k2Star 实现
- `ultralytics/nn/modules/__init__.py` — 模块注册
- `ultralytics/nn/tasks.py` — 模型解析器中添加 Star 模块支持

---

## 九、部署说明

### 9.1 部署打包

```bash
bash deploy_pack.sh
```

生成 `building_detect_deploy.tar.gz`，包含：
- 模型权重（runs/building_seg_v5/weights/best.pt）
- API 应用（api_app/）
- 修改后的 ultralytics 包

### 9.2 部署启动

```bash
# 解压到目标服务器
tar xzf building_detect_deploy.tar.gz --strip-components=1
bash start.sh
```

start.sh 使用 `/root/anaconda3/envs/torch310/bin/python`，端口 9030，设置 YOLO_OFFLINE=True。

---

## 十、关键文件速查表

| 文件 | 路径 | 说明 |
|---|---|---|
| 当前最佳模型 | runs/building_seg_v5/weights/best.pt | 5类YOLO11n-seg, API部署使用 |
| 5类训练脚本 | train_building.py | 当前版本 |
| 2类训练脚本 | train_building_mixed.py | 当前版本 |
| 5类预处理 | preprocess.py | 当前版本 |
| 2类预处理 | preprocess_mixed.py | 当前版本 |
| 变化检测脚本 | detect_changes.py | 可视化版 |
| API 服务 | api_app/app.py | 部署版 |
| API 配置 | api_app/config.py | 阈值/端口 |
| API 核心服务 | api_app/services/change_service.py | 变化检测全流程 |
| 模型配置 | api_app/models/model_config.json | 模型ID/路径/类别 |
| v5数据集配置 | /home/fumu/datadisk/building_seg_v5/data.yaml | 5类 |
| v5_mixed数据集配置 | /home/fumu/datadisk/building_seg_v5_mixed/data.yaml | 2类 |
| 原始标注数据 | /home/fumu/datadisk/标注区域划分/ | 堆龙东嘎+曲水 |
| 旧数据 | /home/fumu/datadisk/drone_data_src/ | 238张 |
| StarNet 模型 | ultralytics/cfg/models/11/yolo11-star.yaml | YOLO11-Star |
| StarBlock 代码 | ultralytics/nn/modules/block.py | 自定义模块 |

---

## 十一、常见操作参考

### 继续训练（从断点恢复）

```bash
yolo segment train resume model=runs/building_seg_v5/weights/last.pt
```

### 用新模型做变化检测

修改 detect_changes.py 中的模型路径：
```python
MODEL_PATH = "runs/你的新模型/weights/best.pt"
```

修改 api_app/models/model_config.json：
```json
{
  "yolo_path": "runs/你的新模型/weights/best.pt",
  "classes": [0, 1, ...]
}
```

### 单张图片预测

```bash
yolo segment predict model=runs/building_seg_v5/weights/best.pt source=你的图片.jpg conf=0.5
```